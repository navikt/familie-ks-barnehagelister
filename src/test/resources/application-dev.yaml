spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/familie-ks-barnehagelister
    password: test
    username: postgres
  flyway:
    enabled: true

prosessering:
  rolle: "928636f4-fd0d-4149-978e-a6fb68bb19de" # 0000-GA-STDAPPS

#TODO: Denne kan mest sannsynlig fjernes
MASKINPORTEN_WELL_KNOWN_URL: http://localhost:${mock-oauth2-server.port}/issuer1/.well-known/openid-configuration
MASKINPORTEN_ISSUER: https://test.maskinporten.no/
AZURE_APP_WELL_KNOWN_URL: http://localhost:${mock-oauth2-server.port}/issuer1/.well-known/openid-configuration

#For å kjøre mot prosessering-frontend lokalt må denne URL-en peke på Azure, og ikke på mock-oauth2-serveren. Det er
# fordi prosessering-frontend henter inn metadata fra denne URL-en, og da må den kunne finne de riktige verdiene for å
# kunne validere tokenet som sendes fra prosessering-backend.
#AZURE_APP_WELL_KNOWN_URL: https://login.microsoftonline.com/navq.onmicrosoft.com/v2.0/.well-known/openid-configuration
#Denne trengs også for lokal kjøring mot prosessering-frontend, for at token skal ha riktig aud.
#AZURE_APP_CLIENT_ID: dummy

godkjente-leverandorer:
  leverandorer:
    - orgno: "310028142"
      navn: "HUMAN DEDIKERT ISBJØRN SA"

